# ============================================================================
# LexAegis AI — Backend dependencies
#
# Pins are grouped by the phase that first consumes them. Phase 1 only needs the
# "Core / Ingress" block, but the full set is declared so later phases install
# nothing new. Install with:  pip install -r requirements.txt
# (or the lighter Phase-1 subset: pip install -r requirements-phase1.txt)
# ============================================================================

# --- Core / Ingress (Phase 1) ----------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
python-multipart==0.0.20
PyJWT[crypto]==2.10.1
httpx==0.28.1
supabase==2.11.0
python-dotenv==1.0.1

# --- Retrieval (Phase 2) ----------------------------------------------------
chromadb==0.5.23
sentence-transformers==3.3.1
FlagEmbedding==1.3.3
rank-bm25==0.2.2
pypdf==5.1.0
python-docx==1.1.2

# --- Agents / LLM (Phase 2/3) ----------------------------------------------
langgraph==0.2.60
langchain-core==0.3.28
ollama==0.4.4

# --- Safety (Phase 2) -------------------------------------------------------
presidio-analyzer==2.2.355
presidio-anonymizer==2.2.355

# --- Observability / Caching (Phase 3) -------------------------------------
arize-phoenix==7.3.2
openinference-instrumentation==0.1.21
opentelemetry-sdk==1.29.0
opentelemetry-exporter-otlp==1.29.0
gptcache==0.1.44

# --- Evaluation (Phase 4) ---------------------------------------------------
ragas==0.2.9
deepeval==2.1.5
